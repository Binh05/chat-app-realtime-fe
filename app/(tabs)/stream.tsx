import { Text, View } from "react-native";

function Stream() {
  return (
    <View
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        flex: 1,
      }}
    >
      <Text>Stream screen</Text>
    </View>
  );
}

export default Stream;
